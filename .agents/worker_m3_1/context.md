# Milestone M3: Rendering & Visual Verification Pipeline

## Mission
Implement `src/powerpoint_mcp/rendering/renderer.py`, `image_diff.py`, `visual_compare.py`, and `__init__.py` according to `PROJECT.md` and `spec_miner_core_1/handoff.md`.
Implement unit and verification tests in `tests/test_rendering.py`.
Verify that PowerPoint COM rendering (on Windows), LibreOffice fallback, pixel diffing, changed bounding boxes, and visual comparison tests pass 100%.
