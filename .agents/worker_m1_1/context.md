# Milestone M1: Core Models & PPTX Inspection Engine

## Mission
Implement `src/powerpoint_mcp/models/` and `src/powerpoint_mcp/pptx/inspector.py`, `styles.py`, and `relationships.py` according to `PROJECT.md` and `spec_miner_core_1/handoff.md`.
Write unit tests in `tests/test_inspection.py`.
Verify that `pytest tests/test_inspection.py` passes 100%.
